#include "war.h"

namespace devils_engine {
  namespace core {
    war::war() : cb(nullptr), war_opener(nullptr), target_character(nullptr), opener_realm(nullptr), target_realm(nullptr), claimat(nullptr) {}
    
    
  }
}
